<%= form_with(model: product, local: true, class: "productForm") do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, class: 'formLabel' %>
    <%= form.text_field :name, class: 'formField' %>
  </div>

  <div class="field">
    <%= form.label :description, class: 'formLabel' %>
    <%= form.text_area :description, class: 'formField' %>
  </div>

  <div class="field">
    <%= form.label :image_url, class: 'formLabel' %>
    <%= form.text_field :image_url, class: 'formField' %>
  </div>

  <div class="field">
    <%= form.label :colour, class: 'formLabel' %>
    <%= form.text_field :colour, class: 'formField' %>
  </div>

  <div class="field">
    <%= form.label :price, class: 'formLabel' %>
    <%= form.text_field :price, class: 'formField' %>
  </div>

  <div class="actions submit-btn">
    <%= form.submit class: 'btn btn-small'%>
  </div>
<% end %>
